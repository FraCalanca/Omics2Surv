% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/train_cox_glmnet_LASSO.R
\name{train_cox_glmnet_LASSO}
\alias{train_cox_glmnet_LASSO}
\title{Train a Cox LASSO Model with Cross-Validation}
\usage{
train_cox_glmnet_LASSO(
  x_training,
  y_training,
  nfolds = 5,
  optimal_lambda = "minimum",
  type_measure = "C",
  penalty = NULL,
  system_type = "unix",
  nlambda = 200,
  seed = 123
)
}
\arguments{
\item{x_training}{A matrix or data frame of predictors (e.g., omics data).}

\item{y_training}{A data frame containing survival outcomes, specifically
\code{overall_survival} (time) and \code{status} (event).}

\item{nfolds}{Integer. Number of folds for cross-validation. Default is 5.}

\item{optimal_lambda}{Character. Choice of lambda for prediction:
\code{"minimum"} (lambda.min) or \code{"1se"} (lambda.1se).}

\item{type_measure}{Character. Loss function for cross-validation.
Default is \code{"C"} (C-index).}

\item{penalty}{Optional numeric vector for penalty factors (e.g., for Adaptive LASSO).}

\item{system_type}{Character. Operating system type, either \code{"unix"}
or \code{"windows"}, to handle parallel backend registration.}

\item{nlambda}{Integer. The number of \eqn{\lambda} values in the sequence.
Default is 200.}

\item{seed}{Integer. Seed for reproducibility of cross-validation folds.}
}
\value{
A named list containing:
\itemize{
\item \code{optimal_lambda}: The selection rule used.
\item \code{cv_fit}: The fitted \code{cv.glmnet} object.
\item \code{selected_lambda}: The numeric value of the chosen \eqn{\lambda}.
\item \code{selected_features}: Character vector of predictors with non-zero coefficients.
\item \code{pred}: Linear predictors for the training set.
\item \code{cindex}: Training set Concordance Index.
}
}
\description{
Fits a Cox proportional hazards model using LASSO regularization (L1 penalty)
with k-fold cross-validation and parallel processing support.
}
