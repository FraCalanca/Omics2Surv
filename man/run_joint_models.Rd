% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_joint_models.r
\name{run_joint_models}
\alias{run_joint_models}
\title{Run Joint Multi-Omics Survival Models}
\usage{
run_joint_models(
  omics_list,
  clinical_data,
  surv_list,
  joint_method = c("CoopCox", "AFTCoop", "blockForest", "flexynesis"),
  train_fraction = 0.7,
  seed = 123,
  ...
)
}
\arguments{
\item{omics_list}{Named list of SummarizedExperiment objects, each representing
an omics layer (e.g., transcriptomics, proteomics).}

\item{clinical_data}{Matrix or data frame of clinical data (variables as rows,
samples as columns).}

\item{surv_list}{Character vector of survival variable names in clinical data
(e.g., \code{c("overall_survival", "status")}).}

\item{joint_method}{Character string specifying the joint model: \code{"CoopCox"}
(2-3 omics), \code{"AFTCoop"} (exactly 2 omics), \code{"blockForest"} (any number),
or \code{"flexynesis"}.}

\item{train_fraction}{Numeric proportion (0-1) of samples for training. Default is 0.7.}

\item{seed}{Integer seed for reproducibility. Default is 123.}

\item{...}{Additional arguments passed to the specific joint modeling function.}
}
\value{
A MultiAssayExperiment object with results stored in
\code{metadata(mae)$analysis$cooperative[[joint_method]]} containing:
\itemize{
\item \code{train}: Training results (model, predictions, C-index, selected features)
\item \code{test}: Test results (predictions, C-index)
}
}
\description{
Performs end-to-end joint modeling workflow: builds a MultiAssayExperiment from
omics data, prepares design matrices, splits data into train/test sets, and fits
a specified joint integration model.
}
