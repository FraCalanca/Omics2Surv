% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/train_cox_glmnet_ElasticNet.R
\name{train_cox_glmnet_ElasticNet}
\alias{train_cox_glmnet_ElasticNet}
\title{Train a Cox Elastic Net Model with Alpha Optimization}
\usage{
train_cox_glmnet_ElasticNet(
  x_training,
  y_training,
  nfolds = 5,
  optimal_lambda = "minimum",
  type_measure = "C",
  alpha_range = seq(0.1, 0.9, by = 0.1),
  penalty = NULL,
  system_type = "unix",
  nlambda = 200,
  seed = 123
)
}
\arguments{
\item{x_training}{A matrix or data frame of predictor variables.}

\item{y_training}{A data frame containing survival outcomes with columns
\code{overall_survival} and \code{status}.}

\item{nfolds}{Integer. Number of folds for cross-validation. Default is 5.}

\item{optimal_lambda}{Character. Rule for lambda selection: \code{"minimum"}
(lambda.min) or \code{"1se"} (lambda.1se).}

\item{type_measure}{Character. Performance metric for CV (e.g., \code{"C"}
for C-index or \code{"deviance"}).}

\item{alpha_range}{Numeric vector. The sequence of \code{alpha} values (0 to 1)
to test. Default is 0.1 to 0.9.}

\item{penalty}{Optional numeric vector of penalty factors for individual features.}

\item{system_type}{Character. Operating system: \code{"unix"} or \code{"windows"}.}

\item{nlambda}{Integer. Number of \eqn{\lambda} values in each \code{glmnet} path.
Default is 200.}

\item{seed}{Integer. Seed for reproducibility.}
}
\value{
A named list containing:
\itemize{
\item \code{cv_fit}: The \code{cv.glmnet} object for the best performing \code{alpha}.
\item \code{best_alpha}: The alpha value that yielded the best CV result.
\item \code{selected_lambda}: The numeric \eqn{\lambda} used for the final model.
\item \code{selected_features}: Predictors with non-zero coefficients at the chosen alpha/lambda.
\item \code{pred}: Linear predictors for the training set.
\item \code{cindex}: Training set Concordance Index.
}
}
\description{
Fits a Cox proportional hazards model using Elastic Net regularization. The function
iterates through a range of \code{alpha} values to identify the optimal mixture
of L1 and L2 penalties based on cross-validated performance.
}
